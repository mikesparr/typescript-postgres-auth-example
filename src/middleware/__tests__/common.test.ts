describe("middleware", () => {
  describe("handleCors", () => {
    it("is magic", () => undefined);
  });

  describe("handleBodyRequestParsing", () => {
    it("is magic", () => undefined);
  });

  describe("handleCompression", () => {
    it("is magic", () => undefined);
  });
});
